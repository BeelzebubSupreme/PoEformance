# Runtime data
dumped_tables/
preload_dumps/
debug/

# Upstream-Cache (auto-cloned by ahk/OffsetCompare.ahk)
.upstream_cache/

# Python cache
__pycache__/
*.pyc

# IDE / Editor
.vscode/
.continue/

# Per-user Claude Code settings
.claude/settings.local.json

# Logs & temp files
logs/
*.error.log
*.error.log.*

# Runtime-generated (user-specific)
offset_history.json
# legacy — PatchChecker now stores this in the config INI
last_known_patch.txt
poeformance_config.ini
# legacy name (pre-PoEformance rebrand; auto-migrated on startup)
gamehelper_config.ini
# EntityAlerts self-persist (user-specific)
alerts.ini
hotkeys.json
hotkeys_export/
desktop.ini

# LootTracker self-persist (user-specific) + generated price cache
sessions/
data/loot_prices.tsv
data/loot_prices.tsv.tmp

# Trade-API unique pricing — WebView2 login profile (holds the session cookies) + price cache
# (NEVER commit). The whole config/ dir is user-local: wv2_poe/ is the trade-session browser
# profile where POESESSID / cf_clearance live; they never leave the browser.
config/
data/trade_prices.tsv
data/trade_prices.tsv.tmp

# Dev notes (not for repo)
NEXT_STEPS.md
QUICKSTART.md
STAT_EXTRACTION_README.md

# Raw extraction output (regenerated from game files)
data/raw_csv/
data/raw_extracted/

# Local-only dev / test entrypoints (not part of the shipped helper)
dev/

# Compiled output (built locally, not shipped in the repo)
PoEformance.exe
WebView2Loader.dll

# Skill-node icon build cache (downloaded GGG sprite sheets)
Tools/.skilltree_cache/
